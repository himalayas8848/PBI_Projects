#For a new project in Local Github folder, first cd github folder.
# Clone the url created before in github: git clone https://github.com/himalayas8848/Realtime_Map.git
# The cd Realtime_Map folder to work from there.

# Else 
git pull origin master

# Crate dev folder and go to the developer folder
git checkout -b dev

# If you want to delete a branch
git branch -D dev

# Make the change from now, then
git add .
git commit -m "notes"

# ATTENTION: Push to developement enviroment
git push origin dev

# Then go to github web to create pull request


